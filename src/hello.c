//Primeiro arquivo em liguagem C
//Vamos importar a biblioteca base para executar o nosso programa
//a biblioteca se chama stdio(std>Standard | 1 > input | 0 > output)
//para importar, usaremos o comandos #include
#include <stdio.h>

//abaixo, iremos escrever os comandos que serao realmente executados 
//utlizaremos a funçao main(pricipal)para iniciar a execuçao
int main (){
    // O comando printf exibe uma menagem em tela de console 
    printf("Hello, World");
// Informa ao compilador(tradudor)gcc que o programa acabou 
    return 0;
}
